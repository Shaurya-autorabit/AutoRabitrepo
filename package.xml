<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <version>61.0</version>
    <types>
        <!-- Existing classes -->
        <members>A0</members>
        <members>A000</members>
        <members>ChangePasswordController</members>
        <members>ChangePasswordControllerTest</members>
        <members>OuterClass</members>

        <!-- Simulated invalid classes -->
        <members>NonExistentClass</members> <!-- This class does not exist -->
        <members>InvalidOuterClass.InnerClass</members> <!-- Invalid inner class reference -->
        <members>DeprecatedClass</members> <!-- Reference a known deprecated class -->
        <name>ApexClass</name>
    </types>
    <types>
        <!-- Invalid or missing layouts -->
        <members>Account-NonExistentLayout</members> <!-- Layout that doesn't exist -->
        <members>Opportunity-InvalidLayout</members> <!-- Referencing a layout incorrectly -->
        <name>Layout</name>
    </types>
    <types>
        <!-- Invalid or missing profiles -->
        <members>NonExistentProfile</members> <!-- This profile does not exist -->
        <members>InvalidProfile</members> <!-- Reference an invalid profile -->
        <name>Profile</name>
    </types>
    <types>
        <!-- Invalid or missing objects -->
        <members>NonExistentCustomObject__c</members> <!-- This custom object does not exist -->
        <members>Account.NonExistentField__c</members> <!-- Referencing an invalid field -->
        <name>CustomObject</name>
    </types>
    <types>
        <!-- Invalid permissions -->
        <members>NonExistentPermissionSet</members> <!-- Invalid permission set -->
        <members>InvalidPermissionSet</members> <!-- Another invalid permission set -->
        <name>PermissionSet</name>
    </types>
    <types>
        <!-- Invalid workflows -->
        <members>InvalidWorkflow</members> <!-- Workflow rule that does not exist -->
        <name>Workflow</name>
    </types>
    <types>
        <!-- Invalid triggers -->
        <members>InvalidTrigger</members> <!-- Trigger that does not exist -->
        <name>ApexTrigger</name>
    </types>
</Package>
